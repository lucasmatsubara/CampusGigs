spring.application.name=campus-gigs

# A conexao com o Postgres e resolvida automaticamente pelo modulo
# spring-boot-docker-compose a partir do compose.yaml na raiz do projeto.
# Nao e necessario configurar spring.datasource.* manualmente em dev.

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
